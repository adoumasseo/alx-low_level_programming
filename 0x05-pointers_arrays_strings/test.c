#include <stdio.h>

void main(void)
{
	char *a;
	char *w = "withney est très belle nous aurons de beaux gosses";
	printf("%ls\n", i);
	a = "je suis donc trop beau";
	printf("%s", a);
	printf("%s", w);
}
